# 스마트 포도 선별기 현장 도입 및 개발 환경 기술서

본 문서는 '스마트 포도 선별기' 프로젝트를 실제 현장(공장, 농장 등)에 안정적으로 배포하고 유지/보수하기 위한 하드웨어, 소프트웨어, 배포 프로세스의 기준 규격을 정의합니다.

## 1. 하드웨어(Hardware) 환경 명세

### 1-1. 메인 프로세서 (제어 및 UI 렌더링)
- **보드 규격**: Raspberry Pi 3 Model B (또는 4B 호환 권장)
- **메모리(RAM)**: 최소 1GB 이상 (권장 2GB 이상)
- **저장매체**: MicroSD 카드 Class 10 이상 (용량 16GB 이상 권장)
- **디스플레이**: 7인치 터치스크린 (해상도 800x480). UI는 800x480 고정이며 그 외 해상도는 지원하지 않음
- **입력 장치**: 터치스크린이 유일한 입출력 수단. 키보드/마우스 없음
- **네트워크**: 이더넷 연결 또는 신뢰도 높은 무선 Wi-Fi 허브 구비 (OTA 업데이트 시 필수 요구)

### 1-2. 서브 컨트롤러 (데이터 수집 및 하드웨어 통신 제어)
- **보드 규격**: Arduino Mega 2560 (CH340 또는 정품 USB 직렬 드라이버 칩셋 탑재형)
- **통신 연결**: USB to Serial 케이블 (A-to-B 타입), 외부 전원 공급 어댑터 권장 (9V 1A)
- **I/O 인터페이스**: 포도 트레이에 부착된 12채널 로드셀 ADC(HX711) 및 채널별 LED 알림 출력 핀 12개

### 1-3. 저울 물리 배치
실물은 **2행 6열**입니다. 아랫줄이 ①~⑥(좌→우), 윗줄이 ⑦~⑫(좌→우)입니다.

```
  ⑦  ⑧  ⑨  ⑩  ⑪  ⑫     <- 윗줄
  ①  ②  ③  ④  ⑤  ⑥     <- 아랫줄
```

보정/저울점검 다이얼로그는 이 배치를 그대로 씁니다.
메인 화면만 좌우 분할 레이아웃 때문에 90도 돌려 **6행 2열**(왼쪽 열 ①~⑥, 오른쪽 열 ⑦~⑫)로 표시합니다. 의도된 차이입니다.

## 2. 소프트웨어(Software) 환경 명세

### 2-1. 임베디드 OS (라즈베리파이)
- **운영체제**: Raspberry Pi OS Lite (64-bit 권장, Desktop 환경 제외 CLI 버전 적용 필요)
- **GUI 그래픽 백엔드**: X Server (`xinit`, `x11-xserver-utils`) – 창 관리자(Window Manager) 없이 Kiosk 모드 단독 구동
- **입력 도구**: 터치 이벤트 적용 시 마우스 커서 숨김 모듈 탑재 (`unclutter`)

### 2-2. 애플리케이션 프레임워크 (파이썬)
- **언어 버전**: Python 3.9 이상
- **GUI 렌더링**: PyQt5 (`python3-pyqt5`) - 하드웨어 가속 및 고해상도 안티앨리어싱 스케일 환경 구성
- **시리얼 통신**: PySerial (`python3-serial`, Baudrate 고정 115200bps)
- **조합 연산**: `itertools.combinations` 완전 탐색
- **내장 폰트 리소스**: 라이선스가 자유로운 '나눔바른고딕'(`NanumBarunGothic.ttf`)

### 2-3. 형상 관리 (버전 관리 시스템)
- **로컬 패키지**: `git` 패키지 설치 필요. 권한 문제 해결을 위해 특정 사용자로 폴더 접근 권한(Ownership) 맞춤 설정

### 2-4. 통신 프로토콜 (라즈베리파이 ↔ 아두이노)

아두이노 → 라즈베리파이 (약 10Hz)

| 형식 | 의미 |
|---|---|
| `<w1, w2, ..., w12>` | 12채널 무게(g). 죽은 채널은 `ERR` |
| `[VER] <해시>` | 펌웨어 버전 (부팅 시 1회, `<VER>` 요청 시) |
| `[TARE] o1,...,o12` | 이번 영점에서 0으로 만든 무게(g). 잘못된 영점 감지용 |
| `[SYSTEM] 영점 조절 완료` | TARE 완료 통보 |

라즈베리파이 → 아두이노

| 형식 | 의미 |
|---|---|
| `<TARE>` | 영점 조절 + 격리 채널 해제 |
| `<VER>` | 펌웨어 버전 요청 |
| `<1,3,5>` | 해당 번호 LED 점등, 나머지 소등 |
| `<>` | 전체 LED 소등 |

**5진 아웃 격리**: 한 채널이 5회 연속 읽기에 실패하면 격리되어 `ERR`로 보고되고 해당 LED가 100ms 주기로 점멸합니다. 영점(TARE)을 실행하면 격리가 해제됩니다.

## 3. 현장 배포 가이드 및 셋업 절차 (Deployment Process)

### Step 1: 기기 OS Flashing 및 네트워크 셋업
1. Raspberry Pi OS Lite를 SD 카드에 굽습니다. (Raspberry Pi Imager의 고급 설정에서 `SSH 활성화` 및 `Wi-Fi` 미리 설정 포함)
2. 첫 구동 시, 터미널 로그인 후 `sudo apt update && sudo apt upgrade -y` 로 커널 및 베이스 패키지 갱신.

### Step 2: 자동 Kiosk 인스톨 구성
1. 저장소를 라즈베리파이에 복제합니다. 위치는 자유입니다.
   ```
   git clone https://github.com/kjt7942/grape-sorter.git
   cd grape-sorter
   ```
2. `bash setup_kiosk.sh` 실행. (소요 시간 5분 내외)

   스크립트가 하는 일:
   - X 서버, PyQt5, PySerial, git, unclutter 설치
   - `~/.xinitrc` 작성 — **스크립트가 있는 디렉토리를 앱 경로로 자동 설정**하므로 경로를 손으로 맞출 필요가 없습니다
   - `/etc/sudoers.d/grape-sorter` 작성 — `reboot`, `shutdown` 두 명령만 비밀번호 없이 허용 (화면 ⑪·⑫ 카드 기능에 필요)
   - `~/.bash_profile` 에 `startx` 추가

### Step 2-2: 부팅 스플래시 화면 (선택)
```
sudo bash setup_boot_screen.sh
```
`bootimg.png`를 Plymouth 테마로 등록합니다.

> **주의**: 스플래시가 실제로 보이려면 `/boot/cmdline.txt`(또는 `/boot/firmware/cmdline.txt`)에 `quiet splash`가 들어 있어야 합니다. 스크립트는 이 파일을 건드리지 않으므로, 부팅 로그가 그대로 흐른다면 직접 추가하세요.

### Step 3: 부팅 콘솔 환경 통제
1. `sudo raspi-config` 명령어 실행
2. `<1 System Options>` -> `<S5 Boot / Auto Login>` 메뉴 진입
3. `<B2 Console Autologin Text console, automatically logged in as 'pi' user>` 활성화 처리
   > 부팅 직후 X 서버가 기동되고 파이썬 앱이 전체화면으로 실행됩니다.
   > 앱이 비정상 종료되면 `.xinitrc`가 3초 후 자동으로 다시 띄웁니다. 화면 ① 카드를 더블탭한 정상 종료일 때만 콘솔로 빠집니다.

### Step 4: 원격 업데이트 (OTA) 설정 유의점
메인 애플리케이션(`main.py`)은 시작 시 백그라운드로 업데이트를 확인합니다. 안전히 다루려면:
- 로컬 저장소가 실제 원격 브랜치(origin/main)를 추적하고 있어야 합니다.
- SSH Key 혹은 GitHub PAT 형태의 Credential이 라즈베리파이에 저장되어 패스워드 입력을 건너뛰도록 구성해야 합니다.
- `git pull` 이후 `arduino-cli`가 설치되어 있고 아두이노가 연결돼 있으면 펌웨어도 자동으로 다시 올립니다.
- 업데이트가 실패해도 앱은 기존 버전으로 계속 동작하며, 화면에 실패 사유가 표시됩니다.

**자동 롤백**: `git pull` 직후 `test_sorter.py`를 실행해 새 코드를 검증합니다. 실패하면 pull 직전 커밋으로 `git reset --hard` 하고 기존 버전을 계속 씁니다. 깨진 코드를 푸시해도 현장 기기가 크래시 루프에 빠지지 않습니다. 검증 내용은 `sorter.log`에 남습니다.

> 조합 로직을 수정할 때는 `test_sorter.py`도 함께 갱신하세요. 이 파일이 OTA의 안전장치입니다.

## 4. 운영 중 화면 동작

### 4-1. 숨은 기능 (카드 더블탭)
| 카드 | 동작 |
|---|---|
| ① | 프로그램 종료 (콘솔로 빠짐) |
| ⑥ | 저울 보정 화면 |
| ⑦ | 프로그램 재시작 |
| ⑪ | 라즈베리파이 재부팅 |
| ⑫ | 시스템 전원 끄기 |

### 4-2. 설정 항목
| 항목 | 범위 | 비고 |
|---|---|---|
| 목표무게 | 100g 이상 | ± 버튼 길게 누르면 5배속 |
| 허용오차 | 0 ~ 500g | 목표무게 **초과** 허용치. 기본 50g |
| 최소조합 | 1 ~ 12 | |
| 최대조합 | 1 ~ 12 | |

조합 규칙: 합계가 `목표무게` 이상이고 `목표무게 + 허용오차` 이하인 조합 중 초과분이 가장 작은 것을 선택합니다. 초과분이 같으면 저울을 더 많이 쓰는 조합을 택합니다. **목표에 미달하는 조합은 절대 선택하지 않습니다.**

목표무게·허용오차·최소·최대 네 값은 제품 슬롯 A~H에 함께 저장됩니다. (제품등록 화면에서 슬롯을 2초간 길게 누르면 저장. "저장됨!"이 잠깐 뜬 뒤 저장된 내용이 그 자리에 표시됩니다.)

### 4-3. 조합무게 카드 터치 = 다른 조합 요청
현재 제시된 조합이 마음에 들지 않으면(송이가 상했다 등) 조합무게 카드를 터치하세요. 그 조합을 거절 목록에 넣고 **차선 조합**을 제시합니다. 계속 누르면 후보를 순환하고, 후보가 떨어지면 처음부터 다시 돕니다.

거절 목록은 저울 구성이 바뀌면(송이를 올리거나 내리면) 자동으로 비워집니다. 무게가 노이즈로 흔들리는 것은 구성 변화로 보지 않습니다.

> 시뮬레이션 모드에서는 대신 12개 저울 무게를 전부 새로 만듭니다. 개발용 동작이며 실기와 다릅니다.

### 4-4. 조합실패 시 표시
조합에 실패하면 `조합실패` 옆에 **얼마나 모자란지**가 함께 나옵니다 (예: `70g 부족`). 목표 미달 조합 중 가장 큰 합계와 목표의 차이입니다.

- 부족분이 작다 → 송이를 하나 더 올린다
- 부족분이 크다 → 목표무게·최대조합·허용오차 설정을 확인한다

이 값은 참고 표시일 뿐이며, 미달 조합이 출하 후보로 선택되는 일은 없습니다.

### 4-5. 시작 준비 절차 (전원 순서 무관)

라즈베리파이와 아두이노는 전원이 분리되어 있어 켜는 순서가 정해져 있지 않습니다. 어느 쪽을 먼저 켜도 **USB 연결이 성립하는 순간** 아래 절차가 자동으로 돕니다.

```
전체 LED 깜빡 ×2   (0.6초)   화면: "저울 준비중 / 접시를 비우고 손을 떼세요"
순차 1 → 12       (1.4초)   채널 하나씩 훑기 (배선 확인도 겸함)
영점(TARE) 실행    (~2초)    화면: "저울 영점 잡는 중" — 이 구간은 LED 정지
전체 LED 깜빡 ×3   (0.8초)   화면: "저울 준비 완료"
정상 운전 시작
```

절차가 도는 동안 조합 연산과 LED 출력이 멈춥니다.

**LED가 영점 중에 멈추는 이유**: 아두이노 `performTare()`가 도는 약 2초 동안 `loop()`이 실행되지 않아 시리얼 명령을 받지 못합니다. 그래서 순차 훑기를 영점 **전에** 끝냅니다.

**저장된 보정값**은 `settings.json`에서 앱 시작 시 그대로 읽습니다. 별도 절차가 필요 없습니다. 절차가 하는 일은 **영점(접시 무게를 0으로)** 뿐입니다.

**절차가 다시 도는 경우**
- USB 연결이 성립할 때 (부팅 순서 무관, 재연결 포함)
- 운전 중 아두이노가 재시작됐을 때. 요청하지 않았는데 `[VER]`이 도착하면 재부팅으로 판단합니다. 이때 아두이노가 스스로 영점을 다시 잡았으므로 확인이 필요합니다

### 4-6. 잘못된 영점 감지

**접시에 포도가 올려진 채로 영점을 잡으면 그 무게가 0이 되어, 이후 모든 측정이 그만큼 낮게 나옵니다.** 조용히 미달 박스가 나가는 유일한 경로라 별도로 막습니다.

영점을 마칠 때마다 아두이노가 `[TARE] off1,...,off12`로 **이번에 0으로 만든 무게(g)**를 보고합니다. 앱은 이 값을 `settings.json`에 저장했다가 다음 영점 때 비교합니다.

- 접시만 올려져 있으면 매번 비슷한 값 (예: 250g 내외)
- 포도가 올려진 채 영점을 잡으면 크게 튐 (예: 900g)

**100g 이상 차이 나면** 화면에 경고합니다.

> 주의: 3번 접시에 물건이 올려진 채 영점을 잡았을 수 있습니다.
> 접시를 비우고 영점을 다시 잡으세요.

첫 실행에는 비교 기준이 없으므로 검사를 건너뛰고 기준값만 저장합니다.

### 4-7. 시뮬레이션 모드 제한

가짜 무게를 만드는 시뮬레이션은 **개발용(윈도우)에서만** 동작합니다. 라즈베리파이에서는 아두이노가 연결될 때까지 전 채널 `에러(ERR)`로 표시하고 가짜 값을 만들지 않습니다.

파이를 먼저 켜고 아두이노를 나중에 켜는 경우, 그 사이 화면에 가짜 무게가 뜨면 조작자가 정상으로 오인하기 때문입니다.

### 4-8. 상태 표시
| 합계 라벨 | 의미 |
|---|---|
| `합계` | 정상 |
| `합계(시뮬)` | 개발용 시뮬레이션. 윈도우에서만 나타남 |
| `저울 연결끊김` | 아두이노 없음(미연결/끊김). 전 채널 ERR 표시, 2초마다 자동 재연결 시도 |

연결이 끊긴 상태에서는 가짜 무게를 만들지 않습니다. 조작자가 정상으로 오인하는 것을 막기 위함입니다.

### 4-9. 생산 실적 (`production.csv`)

잠긴 조합의 저울이 **전부 0이 되는 순간**(= 박스를 담아 간 순간) 한 줄이 기록됩니다.

```csv
시각,슬롯,목표무게,실제무게,초과,저울수,사용저울
2026-08-29 14:32:07,C,2050,2080,30,3,"1 10 12"
```

- 엑셀에서 바로 열립니다 (UTF-8 BOM)
- 일별 박스 수, 무게 분포, 출하 기록, 미달 클레임 대응에 씁니다
- 실제무게가 목표에 미달하는 행은 원리상 생기지 않습니다
- 통신이 끊겨 전 채널 ERR이 된 경우는 기록하지 않습니다 (0과 -1을 구분)

> 청소 등으로 저울을 비워도 1건 기록될 수 있습니다. 실무상 무해한 수준으로 보고 별도 구분은 하지 않습니다.

**시각 신뢰성**: 라즈베리파이 3B에는 RTC(실시간 시계)가 없습니다. 전원을 끄면 시계가 멈추고, 부팅 시 `fake-hwclock`이 **마지막 종료 시각**을 복원합니다. 며칠 전 날짜가 그럴듯하게 찍히기 때문에 눈으로는 구분되지 않습니다.

네트워크 동기화가 확인되지 않으면 시각 뒤에 표시를 붙입니다.

```csv
2026-08-29 22:53:07 (시각미확인),수동,2050,2060,10,1,6
```

- 판단 근거는 `/run/systemd/timesync/synchronized` 파일의 존재 여부입니다
- `chrony` 등 다른 시각 동기화 도구를 쓰는 환경에서는 판별이 불가능하므로 연도 상식 검사(2025년 이전)로만 넘어갑니다. 없는 근거로 경고하지 않기 위함입니다
- 앱 시작 시 화면에도 한 번 알립니다 (매번 띄우면 무시하게 되므로 세션당 1회)

시각이 중요한 기록이라면 **DS3231 같은 I2C RTC 모듈(2천원대)을 다는 것이 근본 해결책**입니다. 지금은 틀린 시각을 맞는 것처럼 남기지 않는 데까지만 처리합니다.

## 5. 디버깅 및 트러블슈팅

### 5-1. 로그 확인 (먼저 볼 것)
키오스크 모드에서는 콘솔이 X 화면에 가려져 터미널 출력이 보이지 않습니다. 모든 기록은 파일에 남습니다.

```
tail -n 100 ~/grape-sorter/sorter.log      # 경로는 설치 위치에 맞게
```

1MB마다 회전하며 3개까지 보관합니다.

### 5-2. 시리얼 포트 인식 오류
- 증상: 아두이노 USB를 꽂았는데 `⚠ 저울 연결끊김` 또는 시뮬모드 표시
- 포트는 자동 탐색되므로 소스 수정이 필요 없습니다. 먼저 권한을 확인하세요.
  ```
  sudo usermod -a -G dialout $USER   # 적용하려면 재로그인 필요
  ls /dev/ttyA* /dev/ttyU*
  ```
- 그래도 안 되면 `sorter.log`에서 `시리얼 연결 해제` 줄의 사유를 확인하세요.

### 5-3. 특정 저울만 ERR
- 제품등록 화면 → `저울점검` 진입. 채널별 상태와 펌웨어 버전이 표시됩니다.
- 각 채널의 `LED 확인` 버튼을 누르면 해당 로드셀 LED만 켜집니다. 배선을 눈으로 대조하세요.
- 5회 연속 실패로 격리된 채널은 LED가 점멸합니다. 배선 수리 후 메인화면 `영점`을 눌러 격리를 해제하세요.

### 5-4. 무게가 실제와 다름 (저울 보정)

**영점과 보정은 다릅니다.**

| | 하는 일 | 어디서 |
|---|---|---|
| **영점(TARE)** | 접시 무게를 0으로. 매번 새로 잡음 | 메인화면 `영점` 버튼, 시작 시 자동 |
| **보정(배율)** | 분동으로 눈금 맞추기. 한 번 잡으면 저장됨 | 카드 ⑥ 더블탭, 또는 저울점검 → `저울 보정` |

**보정 절차** (기본 분동 **430g**)

1. 보정 화면에 들어가면 **영점을 자동으로 먼저 잡습니다.** 접시를 비우고 손을 뗀 채 기다리세요. 이 동안 버튼이 잠깁니다
2. 영점이 끝나면 버튼이 풀립니다. 위쪽에서 분동 무게를 맞추세요 (± 길게 누르면 100g 단위)
3. **파란색**으로 표시된 저울에 분동을 올립니다
4. 값이 멈추면 `보정 적용`. 최근 여러 샘플의 평균으로 계산하므로 흔들림이 멎은 뒤 누르세요
5. 다음 저울로 자동 이동합니다. 12개를 모두 돌면 끝납니다 (진행 상황이 `3 / 12 번째`로 표시됨)

| 버튼 | 동작 |
|---|---|
| `보정 적용` | 배율 계산 후 저장, 다음 저울로 |
| `건너뛰기` | 이 저울은 기존 배율 유지 |
| `초기화` | 이 저울 배율을 1.0으로 되돌림 (잘못 적용했을 때) |
| `완료` | 보정 화면 종료 |

- 각 카드 아래에 `미보정` 또는 `배율 1.02`가 표시됩니다. 설치할 때 빠뜨린 채널을 찾는 데 씁니다
- 계산된 배율이 **0.5~2.0을 벗어나면 적용을 거부합니다.** 분동을 안 올렸거나, 다른 저울에 올렸거나, 분동 무게를 잘못 입력한 경우입니다. 화면에 측정값이 같이 표시되니 대조하세요
- 배율은 `settings.json`에 저장되며 기기별로 다릅니다. git에 올라가지 않습니다
- 적용/거부/초기화 내역은 모두 `sorter.log`에 남습니다

> 펌웨어의 `calFactors[] = {424.0, ...}`는 HX711 원시 카운트를 그램으로 바꾸는 로드셀 감도이며, 분동 무게와는 무관합니다. 조작자가 건드릴 값이 아닙니다.

### 5-5. 조합 연산 성능
12개 저울의 전체 부분집합은 4,095가지뿐이라 라즈베리파이 3B에서도 연산 부담이 없습니다. 최대조합 값을 높여도 느려지지 않습니다.

### 5-6. 설정이 초기화됨
설정은 3중으로 보호됩니다.

1. 저장은 임시 파일에 쓴 뒤 교체(`os.replace`)하므로 쓰는 도중 전원이 끊겨도 직전 설정이 남습니다
2. 정상 로드에 성공할 때마다 `settings.backup.json`으로 복사됩니다
3. `settings.json`이 깨지면 `settings.json.corrupt`로 옮기고 **백업에서 자동 복구**합니다. 백업도 없을 때만 기본값으로 시작합니다

초기화됐다면 `sorter.log`에서 `설정 불러오기 실패` / `백업에서 설정을 복구했습니다` 줄을 확인하세요.

### 5-7. 자체 점검 실행
아두이노나 실기 없이 개발 PC에서 전부 돌릴 수 있습니다.

```
python3 tests/run_all.py         # 전체
python3 test_sorter.py           # 조합 로직만 (OTA 관문과 동일)
python3 tests/render_screens.py  # 모든 화면을 PNG 로 저장해 눈으로 확인
```

조합 로직을 수정하면 `test_sorter.py` 를 반드시 함께 갱신하세요. OTA 가 이 파일로
새 코드를 검증하고, 실패하면 되돌립니다. 자세한 내용은 `tests/README.md` 참고.

## 6. 보안 유의사항

- OTA는 원격 저장소에서 받은 코드를 검증 없이 실행하고, 아두이노 펌웨어까지 자동으로 굽습니다. **저장소나 GitHub 계정이 탈취되면 배포된 모든 기기에서 임의 코드가 실행됩니다.** 저장소 쓰기 권한자를 최소로 유지하고 계정에 2단계 인증을 적용하세요.
- HTTPS + PAT 방식을 쓰면 토큰이 라즈베리파이에 평문(`~/.git-credentials`)으로 남습니다. 읽기 전용(deploy key, read-only PAT) 자격증명을 쓰고, 기기 분실 시 즉시 폐기하세요.
- `sudo` 권한은 `reboot`, `shutdown` 두 명령으로만 열려 있습니다. 다른 명령을 추가하지 마세요.
